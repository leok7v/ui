name: build-on-push
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - name: setup msbuild
        uses: microsoft/setup-msbuild@v2
      - name: build debug
        run:  msbuild msvc2022\xrt.sln -t:rebuild -verbosity:quiet -property:Configuration=debug
      - name: build release
        run:  msbuild msvc2022\xrt.sln -t:rebuild -verbosity:quiet -property:Configuration=release
      - name: short sha
        id: vars
        run: echo "sha_short=$(git rev-parse --short HEAD)"  >> $GITHUB_OUTPUT
      - name: check short sha
        run: echo ${{ steps.vars.outputs.sha_short }}
      - name: upload release artifact
        uses: actions/upload-artifact@v4
        with:
          name: xrt.release.zip
          path: |
            bin\release\hellow_world.exe
            bin\release\test_rt.exe
          retention-days: 5
      - name: upload debug artifact
        uses: actions/upload-artifact@v4
        with:
          name: xrt.debug.zip
          path: |
            bin\debug\hellow_world.exe
            bin\debug\test_rt.exe
          retention-days: 5